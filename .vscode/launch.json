{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch DearImGuiApp",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/DearImGuiApp.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "externalConsole": true,
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ]
        }
    ]
}

// ### 🧪 Next Steps

// 1. Open VS Code in your project folder.
// 2. Press `Ctrl+Shift+B` to build.
// 3. Press `F5` to run and debug.
